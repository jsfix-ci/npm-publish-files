{
  "name": "npm-publish-files",
  "version": "0.2.0",
  "description": "Copy files npm includes in publish to a sub directory",
  "author": "Iiro Jäppinen <iiro@jappinen.fi> (https://iiro.fi)",
  "license": "MIT",
  "homepage": "https://github.com/iiroj/npm-publish-files",
  "repository": "github:iiroj/npm-publish-files",
  "bugs": "https://github.com/iiroj/npm-publish-files/issues",
  "bin": "cli.js",
  "files": [
    "cli.js"
  ],
  "scripts": {
    "lint": "tslint -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "watch": "rollup -cw",
    "build": "rollup -c",
    "preversion": "npm run lint && npm run build",
    "version": "standard-version",
    "push": "git push --follow-tags origin master",
    "prepublishOnly": "npm run version && npm run push"
  },
  "engines": {
    "node": ">= 8.10"
  },
  "dependencies": {
    "fast-glob": "^2.2.6",
    "fs-extra": "^7.0.1",
    "yargs": "^12.0.5"
  },
  "devDependencies": {
    "@types/fs-extra": "^5.0.4",
    "@types/node": "^10.12.18",
    "@types/yargs": "^12.0.5",
    "prettier": "^1.15.3",
    "rollup": "^1.1.0",
    "rollup-plugin-typescript2": "^0.18.1",
    "standard-version": "^4.4.0",
    "ts-node": "^7.0.1",
    "tslint": "^5.12.0",
    "tslint-config-prettier": "^1.17.0",
    "tslint-config-standard": "^8.0.1",
    "tslint-plugin-prettier": "^2.0.1",
    "tslint-react": "^3.6.0",
    "typescript": "^3.2.2"
  },
  "keywords": [
    "copy",
    "dir",
    "files",
    "ignore",
    "npm",
    "publish"
  ]
}
